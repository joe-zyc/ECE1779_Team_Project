apiVersion: v1
kind: Service
metadata:
  name: openmotor-frontend-service
spec:
  type: LoadBalancer
  externalTrafficPolicy: Local
  selector:
    app: openmotor-frontend
  ports:
    - port: 8080
      targetPort: 8080
