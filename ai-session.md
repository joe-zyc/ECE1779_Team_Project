# AI Sessions for ECE1779 Team Project

## Session 1: DB Test Data Generation and Ingestion Script

### Prompt

```
can u generate some test files in csv format for the tables i have defined in the db schema, and write a python script that can load these csv files into the db. put all those in a test folder under db
```       

### AI's Response

```
Added a new db/test test-fixture area with CSV files for all schema tables:

users.csv
car_listings.csv
buyer_preferences.csv
listing_images.csv
Built load_test_data.py to load those CSVs into Postgres in FK-safe order:

users -> car_listings -> buyer_preferences -> listing_images
Supports optional truncate before load
Uses ON CONFLICT (id) DO NOTHING for safe re-runs
Refactored loader configuration into load_test_data_config.yml:

DB connection settings
data_dir
table-to-CSV mapping
truncate options and truncate order
CLI flags still override config (--config, --db-url, --data-dir, --truncate)
Expanded the dataset to be more realistic:

users: 12 rows
car_listings: 16 rows
buyer_preferences: 15 rows
listing_images: 22 rows
Validated integrity across CSVs so foreign keys won’t break:

all seller_id values point to valid seller users
all buyer_id values point to valid buyer users
all listing_id values in images point to existing listings
no duplicate IDs / invalid enum values in the generated data
```

### What The Team Did With The AI's Response

- We used the AI's response to generate test data for our PostgreSQL database. The AI provided us with a script that creates sample vehicle listings and user data, which we then ingested into our database to facilitate testing and development.
- During the session, the AI was not able to setup the script to read the configuration file defined in YAML, which we made some adjustments to the script to make it work. We also had to adjust the generated CSV files to ensure that the foreign key relationships were valid and that the data was consistent with our database schema.
- We validated the test script and the generated data by connecting to our PostgreSQL database through a database client (DBeaver), and running queries to confirm that the data was correctly inserted and that the relationships between tables were maintained. 

## Session 2: Backend API skeleton

### Prompt
```
based on IMPLEMENTATION_PLAN.md  generate backend template structure inside the backend folder. Do not implement the api, just put a template
```

### AI's Response

```
Here’s the summary of what we did:

Generated a backend template scaffold from IMPLEMENTATION_PLAN.md under ECE1779_Team_Project (no real API logic, mostly 501 Not Implemented placeholders).
Added initial structure for app entry, routes, controllers, middleware, config, and docs.
Removed the email notification/worker parts after your request:
deleted worker files and notification route/controller/service/repo templates
removed related env vars and script references.
You asked what each module was for, so the remaining modules were explained and then the scaffold was simplified by removing unused layers:
removed validators, validation middleware, services, repositories, DB pool placeholder, and empty folders.
package.json was trimmed and adjusted multiple times:
removed unused deps once
restored pg when you said Postgres is needed.
Dependency roles were explained (express, cors, helmet, morgan, dotenv, pg, nodemon).
Latest check confirmed there were no unlisted dependencies being imported in code.
Current backend/package.json now includes:
cors, dotenv, express, helmet, morgan, pg
scripts: start, check.
```

### What The Team Did With The AI's Response
- We utilized the template generated by the AI as a starting point for our backend API development. The AI provided us with a structured codebase that included placeholders for API endpoints, routes, controllers, and middleware, which we then filled in with our actual implementation as we developed the backend.
- One thing that was incorrect in the AI's response was that it included excess package dependencies that were not relevant to our plan. It also tried to hardcode environment variables into the codebase (such as listing to localhost postgres)
- We removed the excess dependencies and adjusted the codebase to ensure that it was aligned with our implementation plan. To verify the correctness, our team setup a discussion to review the architecture and structure of the backend codebase, ensuring that it was organized in a way that facilitated development and maintenance. We also ran the backend server to confirm that it started successfully without any errors, which indicated that the template structure was correctly set up.

## Session 3: Frontend UI Generation

### Prompt
```
based on IMPLEMENTATION_PLAN.md  and  immplementation summary, can u generate a frontend in a separate folder for this app? Follow the frontend development guidelines in AGENTS.md, and make sure to implement the features we have defined in the implementation plan.
```

### AI's Response

```
Frontend work summary:

Set up and repeatedly ran/restarted the frontend dev server (vite) and verified app availability on localhost:5173.
Reworked Seller Hub create flow so image upload is part of the Create Listing form (create listing + upload images in one flow).
Removed the separate per-listing upload UI from Seller Hub cards.
Added/adjusted listing card image display (show car image on tiles without changing API).
Updated listing pagination to 5 listings per page with page navigation controls.
Added/adjusted listing filter behavior, including price range-style filtering.
Removed extra debug/status UI from listings pages (per your requests).
Added unauthenticated listing-detail guard page prompting users to sign in/sign up.
Fixed routing/back navigation issues (including listing detail navigation behavior).
Refined listing detail layout: separated contact section and reduced crowded spec rows.
Removed “report listing” button from UI.
Cleaned Profile page by removing token/user-id debug fields and refresh controls.
Renamed app branding to OpenMotor across visible frontend areas.
Integrated your logo.png into site branding and updated topbar branding details.
Built a new dedicated landing page (/) with logo, platform description, and Login/Sign Up CTAs (instead of using signup page as landing).
Restyled login/signup side panels to be cleaner and more readable.
Tuned landing page visuals: larger centered logo, bigger text/CTAs, centered buttons, and logo placed outside the main content box.
Updated hero panel color theme to navy.
Moved signed-in username display into the topbar next to logout and styled it as a white chip.
Ran build checks (npm run build) after edits to ensure the frontend compiled successfully.
```

### What The Team Did With The AI's Response
- We vibe coded with the AI agent to iteratively develop the frontend UI for our application. The AI is userful as it can quickly adjust to our feedback and make changes to the UI. 
- We tested the frontend by running it locally, interacting with each components, and validate with the backend API and the database to ensure that the system was working as expected. However, the AI made a various of bugs, such as UI layout issues, API integration issues, and routing issues.
- We validated these issues by monitoring the browser console for errors, and used Postman to test the API connectivity. We also adjusted the backend codebase to serve the frontend fuctionalities better. We made a variety of UI adjustments to improve the user experience, such as refining the listing detail layout, adjusting the listing card image display, and tuning the landing page visuals. We also removed some unnecessary UI elements to make the interface cleaner and more user-friendly.